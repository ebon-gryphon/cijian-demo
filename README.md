# 此间 · 我们的关系空间

这是一个根据「双方对自己和彼此的认识 → 关系知识库 → 忙碌时代理接待」构想制作的中文交互 Demo。以异地情侣林屿与许知夏作为虚构示例。

## 一键下载体验

**[下载 Demo 体验包（ZIP）](https://github.com/ebon-gryphon/cijian-demo/releases/latest/download/cijian-demo.zip)** · [查看版本与附件](https://github.com/ebon-gryphon/cijian-demo/releases/latest)

1. 点击上方下载链接，保存 `cijian-demo.zip`（无需登录 GitHub）。
2. 解压整个压缩包，打开 `cijian-demo` 文件夹。
3. 双击 **index.html**，用 Chrome、Edge 或 Safari 开始体验。

无需安装 Node.js、运行命令或填写 API Key。推荐在电脑浏览器体验；手机的文件预览器不一定能运行交互页面。不要直接在压缩包内打开文件。

普通体验者选择 `cijian-demo.zip`，无需下载 GitHub 自动生成的 `Source code` 源码包。如果下载未开始，可进入版本页面，在 **Assets** 中选择体验包。

体验包仅包含 `index.html` 和 `README.txt`。也可以在版本页面直接下载 `index.html`，保存到电脑后用浏览器打开；GitHub 文件预览页面不会运行 Demo。

## 已有源码时打开体验

直接双击本文件夹中的 **打开此间.html**。不需要启动服务，不需要 API Key；页面运行时不调用云端模型。请用 Safari、Chrome 或 Edge 打开。如果系统预览只显示代码，右键选择浏览器打开。

数据仅保存在当前浏览器的本机存储中。浏览器不允许保存时仍可体验，但刷新后可能恢复示例。单文件版本和本地开发预览的存储相互独立。切换身份是演示操作，不是真实账号鉴权。

## 建议体验顺序

1. 默认是许知夏视角，林屿忙碌。点「你几点忙完？」看带来源的接待回复。
2. 点「今天有点委屈」，看消息如何被保留给本人回应；询问未知信息或关系承诺也会转交本人。
3. 点回复下方的记忆来源，查看讲述者和当前记忆内容。
4. 到「彼此的模样」，在「你眼中的我」找到等待回应的理解。可以选择「这符合我的理解」「不太符合」「稍后再说」。不符合时可直接提交，或补充实际感受；卡片中可展开查看双方原话。
5. 到「共同记忆」新增信息。可设为私有或共享；关于对方及共同经历的新增记录需要切换到对方确认后，才进入接待可用范围。添加几个触发词，方便在规则演示中测试。
6. 例子：切换林屿，添加一条关于自己的「晚饭」记忆，内容「今晚想吃番茄面」，触发词「晚饭、吃什么」，打开共享。切换许知夏，问「晚饭吃什么？」。
7. 切换为林屿，进入「回来接着聊」，点「我回来了」，逐条查看并亲自回复。消息会回到同一份对话记录。
8. 聊天顶部自己的头像旁可以「设为忙碌」或「接回」。右侧与「空间设置」中的开关只控制当前身份的助手，不能替另一方设置。
9. 「回忆纪念模式」是独立的远期概念入口，展示一段虚构回忆的可追溯问答，不冒充逝者。

## v0.2.0 更新

- 理解可以确认、否定或稍后回应。新增「存在分歧」「稍后再说」分类，保留回应与当时的原话；作者修改后重新等待确认。
- 回复显示为「林屿（助手）」或「许知夏（助手）」，头像有助手角标；旧聊天中的助手身份也会正确显示，不随体验视角切换而变。
- 自动离开与手动忙碌分开，状态入口放到聊天顶部。双方各自决定是否允许自己的助手接待。

### 自动状态怎么体验

1. 先点击自己的「在线 · 自动切换」或「接回」，确保不是手动忙碌。
2. 切换到另一个浏览器标签页或其他窗口。离开不足 2 分钟属于缓冲期，助手不代回；满 2 分钟后才允许接待新消息。
3. 回到页面，自动接待立即暂停，显示待回应留言数量；不会自动将留言标记为已处理。
4. 如果主动点击「设为忙碌」，短暂返回仍保持忙碌。点击「接回」或本人发送消息，才恢复在线自动模式。
5. 单机模拟双方时，切换体验身份也会让原身份开始离开计时。例如许知夏先设为自动，再切换林屿；等 2 分钟后，林屿发送消息即可看到「许知夏（助手）」回复。切回许知夏后自动接待暂停。

默认示例中的林屿为手动忙碌，方便第一次打开即体验接待。助手关闭时，离开或忙碌只接收留言，不会代回。缓冲期间收到的消息不会在满 2 分钟后被自动补答，仍保留给本人；助手仅接待之后的新消息。

## 已实现

- 四视角认知卡片，保留讲述者和被描述者；认知差异案例与发起沟通。
- 记忆新增/编辑、私人/共享分区、另一方确认、关键词触发。
- 情境规则模拟接待、引用来源、未确认与私有信息排除、未知问题转交。
- 双方身份切换、两分钟自动离开、手动忙碌覆盖、本人逐条回复及留言处理状态。
- 本机保存、移动端适配、键盘发送与中文输入法兼容。
- 可直接打开的单文件 Demo，以及完整 React / TypeScript 源码。

## 演示边界

这是交互原型，不是已接入大模型的聊天服务。AI 由关键词与规则模拟，没有真实 RAG、推理或真人语音克隆。四视角之间的差异提示为固定示例，不是自动心理分析。纪念模式当前只有一段示例。没有真实双人注册、跨设备同步、微信代发或服务端隐私隔离；所有示例都在同一个本机空间中。

引用面板显示记忆的当前版本。停止共享会阻止后续引用，已经发送的聊天内容不会自动删除。

自动状态依据本页可见性与窗口焦点，不代表真实工作状态，不向伴侣展示精确离开时间。后台页面可能被浏览器节流或冻结；关闭页面后不能持续接待。没有跨标签页或跨设备同步，请在一个页面内切换身份体验，勿把这个版本当成已上线的消息服务。

使用原浏览器、原文件位置时，会兼容读取 v0.1.0 的本机记忆和聊天，并迁移忙碌设置。下载到新路径时，浏览器可能使用不同存储空间，原文件仍可保留作备份。

## 源码开发

Node.js 22.13+（建议 24）。下载或克隆源码后，在本文件夹运行 `npm ci`，再运行 `npm run dev`，打开终端显示的本地地址。

- `app/page.tsx`：界面、记忆管理、身份切换、消息交接。
- `app/demo.ts`：数据结构、示例数据和接待规则。
- `app/presence.ts`：自动状态、手动覆盖与页面事件监听。
- `app/globals.css`：视觉样式和响应式布局。
- `scripts/build-offline.mjs`：打包单文件离线版本。
- `tests/reception.test.mjs`：接待证据与隐私规则测试。
- `tests/interaction.test.mjs`：理解回应、身份命名、状态边界与事件测试。

验证与打包：`npx tsc --noEmit`、`node --test tests/*.test.mjs`、`npm run build`、`node scripts/build-offline.mjs`。

页面还在支持 WebMCP 的环境提供读取演示状态、发送演示消息的接口。未进行浏览器端 WebMCP 实测，不影响常规网页操作。

## 制作下载包

修改界面后，先运行 `npm run build` 和 `node scripts/build-offline.mjs` 更新离线页面，再运行 `python3 scripts/package-release.py`（需要 Python 3）。仅重新打包现有离线页面时，可直接运行最后一条命令。

脚本会在 `.releases/` 生成 `cijian-demo.zip`、单独的 `index.html` 和 `SHA256SUMS.txt`。发布 GitHub Release 时上传这三个附件；此目录不会提交进源码。体验包不包含依赖、环境变量、本机缓存或用户的浏览器聊天记录。

## 后续接入真实产品

先接入两人独立身份和服务端访问控制，再实现带来源和权限过滤的检索、模型回复和本人交接。判断产品价值的重点是双方是否愿意使用与是否减少漏接，而非单纯提高聊天时长。

项目通过 GitHub 仓库与 Release 分发，没有部署在线网站。项目保留了初始化的 Sites 关联信息，但注册的空站点未发布。
