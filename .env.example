# Optional server-side OpenAI-compatible credentials. Otherwise use the in-page key (memory only).
DIARY_API_BASE_URL=https://api.openai.com/v1
DIARY_API_KEY=
DIARY_TEXT_MODEL=gpt-5-mini
DIARY_IMAGE_MODEL=gpt-image-2
DIARY_API_PROTOCOL=openai
DIARY_IMAGE_API_BASE_URL=https://api.openai.com/v1
DIARY_IMAGE_API_KEY=
# Comma-separated, explicitly trusted custom base URLs for server proxying.
DIARY_ALLOWED_API_BASES=
